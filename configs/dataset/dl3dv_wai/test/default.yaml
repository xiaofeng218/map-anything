dataset_str:
  "DL3DVWAI(
    split='${dataset.dl3dv_wai.test.split}',
    resolution=${dataset.dl3dv_wai.test.dataset_resolution},
    principal_point_centered=${dataset.dl3dv_wai.test.principal_point_centered},
    aug_crop=${dataset.dl3dv_wai.test.aug_crop},
    transform='${dataset.dl3dv_wai.test.transform}',
    data_norm_type='${dataset.dl3dv_wai.test.data_norm_type}',
    sample_specific_scene=${dataset.dl3dv_wai.test.sample_specific_scene},
    specific_scene_name='${dataset.dl3dv_wai.test.specific_scene_name}',
    ROOT='${dataset.dl3dv_wai.test.ROOT}',
    dataset_metadata_dir='${dataset.dl3dv_wai.test.dataset_metadata_dir}',
    overfit_num_sets=${dataset.dl3dv_wai.test.overfit_num_sets},
    variable_num_views=${dataset.dl3dv_wai.test.variable_num_views},
    num_views=${dataset.dl3dv_wai.test.num_views},
    covisibility_thres=${dataset.dl3dv_wai.test.covisibility_thres},
    mvs_confidence_filter_thres=${dataset.dl3dv_wai.test.mvs_confidence_filter_thres})"

split: 'train'
dataset_resolution: ${dataset.resolution_train}
principal_point_centered: ${dataset.principal_point_centered}
aug_crop: 16
transform: 'imgnorm' #'colorjitter+grayscale+gaublur' # TODO: change it
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
overfit_num_sets: null
variable_num_views: ${dataset.test.variable_num_views}
covisibility_thres: 0.25
mvs_confidence_filter_thres: 0 #0.25 # TODO: change it
sample_specific_scene: true
specific_scene_name: ${specific_scene_name}
num_views: ${dataset.num_views}
